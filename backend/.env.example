# Copy to backend/.env (setup scripts do this automatically).
# Never commit backend/.env — it is gitignored.

SECRET_KEY=dev-secret-key-change-for-production

# Admin dashboard login (seeded on first backend start)
ADMIN_EMAIL=admin@tap.local
ADMIN_PASSWORD=TAPadmin2026
ADMIN_NAME=TAP Admin

# In debug mode, visiting /admin/login can auto-sign-in with the credentials above
DEV_AUTO_LOGIN=true
